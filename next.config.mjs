/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Propuesta-Maco',
  assetPrefix: '/Propuesta-Maco/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
